<Window x:Class="CookMaster.Views.CodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d"
        Title="CodeWindow" Height="200" Width="300">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Generated code:" HorizontalAlignment="Center" Margin="0,0,0,6" FontWeight="DemiBold"/>
        <TextBlock Text="{Binding Code}" FontSize="28" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,12" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Regenerate"
                    Command="{Binding RegenerateCodeCommand}" Padding="12,6" Margin="0,0,12,0"/>
            <Button Content="Copy &amp; Close"
                    Command="{Binding CopyAndCloseCommand}"
                    IsDefault="True" Padding="16,6"
                    Style="{StaticResource ButtonSaveStyle}"/>
            
        </StackPanel>
    </StackPanel>
</Window>
