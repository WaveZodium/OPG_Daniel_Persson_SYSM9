<Window x:Class="CookMaster.Views.RecipeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="RecipeDetailWindow" Height="450" Width="800">
    <StackPanel>
        <Button Content="Save" Command="{Binding PerformSaveCommand}" Width="150" Height="75"/>
        <Button Content="If Admin: Delete" Width="150" Height="75"
                Background="Red"
                Command="{Binding PerformDeleteCommand}" 
                Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}"/>
        <Button Content="Close" Command="{Binding PerformCloseCommand}" Width="150" Height="75"/>
    </StackPanel>
</Window>
