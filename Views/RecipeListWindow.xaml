<Window x:Class="CookMaster.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="RecipeListWindow" Height="450" Width="800">
    <StackPanel>
        <Button Content="If Logged In: Logout" Command="{Binding OpenMainWindowCommand}" Width="150" Height="75"/>
        <Button Content="Add Recipe" Command="{Binding OpenAddRecipeWindowCommand}" Width="150" Height="75"/>
        <Button Content="View Recipe" Command="{Binding OpenRecipeDetailWindowCommand}" Width="150" Height="75"/>
        <Button Content="If Admin: List Users" Command="{Binding OpenUserListWindowCommand}" Width="150" Height="75"/>
    </StackPanel>
</Window>
