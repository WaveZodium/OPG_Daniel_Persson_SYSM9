<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CookMaster - Add New Recipe" Height="530" Width="800"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource {x:Type Window}}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
            <Image Source="{StaticResource SteamingPotImage}" HorizontalAlignment="Center"
                   Width="30" Height="30" Stretch="Uniform" Margin="0,0,0,0"/>
            <TextBlock Text="Add New Recipe" FontSize="20" FontWeight="Bold" Margin="4" />
        </StackPanel>

        <!-- Right column: buttons -->
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Margin="4,0,0,0">
            <Button Content="Add Recipe" Width="160" Height="30" Margin="4" 
                    ToolTip="Save changes and close the window"
                    Command="{Binding PerformAddCommand}"
                    Style="{StaticResource ButtonSaveStyle}"/>

            <Button Content="Cancel" Width="160" Height="30" Margin="4"
                    ToolTip="Close without saving changes"
                    Command="{Binding PerformCancelCommand}"
                    Style="{StaticResource ButtonLightGrayStyle}"/>
        </StackPanel>

        <!-- Left column: recipe details -->
        <ScrollViewer Grid.Row="2" Grid.Column="0"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Disabled"
              CanContentScroll="True">
            <StackPanel>
                <TextBlock Text="Select a template recipe to pre-fill the fields:" Margin="4"/>
                <ComboBox ItemsSource="{Binding TemplateRecipes}"
                          SelectedItem="{Binding SelectedTemplate, Mode=TwoWay}"
                          DisplayMemberPath="Title"/>
                
                <TextBlock Text="Title" Margin="4"/>
                <TextBox Margin="4"
                     Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Ingredients" Margin="4"/>
                <ListBox ItemsSource="{Binding Ingredients}" SelectedItem="{Binding SelectedIngredient}" Height="100" Margin="4"/>

                <!-- Add / Remove controls -->
                <StackPanel Orientation="Horizontal" Margin="4">
                    <TextBox Width="300" Margin="0,0,4,0"
                         Text="{Binding NewIngredientText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Add" Width="80"
                        Command="{Binding AddIngredientCommand}"
                        Margin="0,0,4,0"/>
                    <Button Content="Remove" Width="80"
                        Command="{Binding RemoveIngredientCommand}"/>
                </StackPanel>

                <TextBlock Text="Instructions" Margin="4"/>
                <TextBox Margin="4" Height="100"
                     Text="{Binding Instructions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>

                <TextBlock Text="Category" Margin="4"/>
                <ComboBox Margin="4"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
